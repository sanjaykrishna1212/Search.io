import { SearchIoPipe } from './search-io.pipe';

describe('SearchIoPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchIoPipe();
    expect(pipe).toBeTruthy();
  });
});
